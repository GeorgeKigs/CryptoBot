const mongoose = require('mongoose');
// const stripe = require('stripe')

const bankTransactionSchema = new mongoose.Schema({

})

module.exports = mongoose.model('bankTransactionModel',bankTransactionSchema)